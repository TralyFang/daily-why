name = "daily-why-cron-content"
main = "worker.js"
compatibility_date = "2026-06-20"

# Run at 00:05 Beijing time (UTC 16:05 previous day)
[triggers]
crons = ["5 16 * * *"]

# Service binding to the main daily-why worker
[[services]]
binding = "MAIN_APP"
service = "daily-why"
